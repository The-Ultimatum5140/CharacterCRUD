<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>All Characters</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-zinc-900 to-zinc-800 min-h-screen text-white px-6 py-10">
  <div class="max-w-6xl mx-auto">
    <div class="flex justify-between items-center mb-8">
      <h1 class="text-4xl font-bold">All Characters</h1>
      <a href="/" class="text-blue-400 hover:underline text-lg">🏠 Back to Home</a>
    </div>

    <% if (allusers.length === 0) { %>
      <p class="text-zinc-300 text-lg">No characters found. Try adding one from the home page.</p>
    <% } else { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
        <% allusers.forEach(function(user) { %>
          <div class="bg-zinc-800 p-4 rounded-xl shadow-lg hover:shadow-2xl transition duration-300">
            <div class="w-full h-48 bg-zinc-700 rounded-lg overflow-hidden mb-4">
              <img src="<%= user.url || 'https://via.placeholder.com/300x200?text=No+Image' %>" alt="<%= user.name %>" class="w-full h-full object-cover">
            </div>
            <h3 class="text-xl font-semibold mb-1"><%= user.name %></h3>
            <p class="text-zinc-400 mb-4"><%= user.email %></p>
            <div class="flex justify-between">
              <a href="/edit/<%=user._id%>" class="text-blue-400 hover:underline">✏️ Edit</a>
              <a href="/delete/<%= user._id %>" class="text-red-500 hover:underline">🗑️ Delete</a>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>
</body>
</html>